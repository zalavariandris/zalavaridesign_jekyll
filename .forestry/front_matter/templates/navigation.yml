---
label: Navigation
hide_body: true
fields:
- type: field_group_list
  name: Navigation
  label: Navigation
  fields:
  - type: text
    name: name
    label: name
  - type: text
    name: link
    label: link
  - type: field_group_list
    name: children
    label: children
    fields:
    - type: text
      name: name
      label: name
    - type: text
      name: link
      label: link
    config:
      labelField: name
- name: new_menu
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: simple
  label: new menu
